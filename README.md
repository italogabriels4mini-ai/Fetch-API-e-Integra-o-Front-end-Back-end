# 🎬 Catálogo de Filmes — Prática Aula 37
CRUD de filmes com fetch API (sem recarregar página!).

## npm install && npm run dev

## 11 TODOs
1-4. apiRoutes.ts — GET/POST/DELETE /api/filmes
5-6. app.ts — importar e registrar apiRoutes
7-11. catalogo.js — fetch GET, exibir DOM, fetch POST, DELETE, filtro
